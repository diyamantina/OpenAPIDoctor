openapi: 3.1.0
info:
  title: Clean Spec
  version: 1.0.0
servers:
  - url: http://localhost:8080
    description: Local
paths:
  /ping:
    get:
      operationId: ping
      responses:
        '200':
          description: pong
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok: { type: boolean }
